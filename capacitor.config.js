import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'starter-vpv-ionic-vue-js',
  webDir: 'dist'
};

export default config;
